# frozen_string_literal: true
require 'bundler/gem_tasks'
require 'rspec/core/rake_task'
require 'pry'

RSpec::Core::RakeTask.new(:spec)

desc 'Bring up a console in the current project context'
task :console do
end

task default: :spec
